package org.example.g1101_1200.s1103_distribute_candies_to_people;

// #Easy #Math #Simulation

/**
 * 1103. Distribute Candies to People
 *
 * Нам нужно распределить candies конфет среди num_people
 * людей следующим циклическим образом:
 * Раздаем конфеты по очереди: человеку 1, затем человеку 2, ...,
 * человеку num_people, затем снова человеку 1 и так далее.
 * В каждом раунде количество раздаваемых конфет увеличивается на 1:
 * Первый раунд: 1 конфета первому человеку
 * Второй раунд: 2 конфеты второму человеку
 * И так далее...
 * Когда остаток конфет меньше, чем требуется для следующей раздачи,
 * отдаем все оставшиеся конфеты следующему человеку и останавливаемся.
 *
 * (примеры внизу)
 *
 * Решение - итеративный подход - 3-е решение - 2 ms
 *
 */
class Solution {
    public int[] distributeCandies(int candies, int num_people) {
        int[] result = new int[num_people];
        int currentCandy = 1;
        int index = 0;

        while (candies > 0) {
            // Определяем, сколько конфет отдать в текущей итерации
            int give = Math.min(currentCandy, candies);

            // Добавляем конфеты текущему человеку
            result[index] += give;

            // Обновляем остаток конфет
            candies -= give;

            // Переходим к следующему человеку и следующему количеству конфет
            index = (index + 1) % num_people;
            currentCandy++;
        }

        return result;
    }
}
/*
Пример 1
Ввод: candies = 7, num_people = 4
Вывод: [1,2,3,1]
Объяснение:
1-й человек: 1 конфета
2-й человек: 2 конфеты
3-й человек: 3 конфеты
4-й человек: 1 конфета (осталось только 1)

Пример 2
Ввод: candies = 10, num_people = 3
Вывод: [5,2,3]
Объяснение:
1-й: 1
2-й: 2
3-й: 3
1-й: 4 (но осталось только 4, так как 1+2+3=6, осталось 4)


 */
