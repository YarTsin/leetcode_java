package org.example.g0101_0200.s0141_linked_list_cycle;

/**
 * Это вспомогательный класс для решения.
 * На литкоде он уже есть его на проверку отправлять не надо,
 * только класс Solution
 */
 class ListNode {
      int val;
      ListNode next;

      ListNode(int x) {
          val = x;
          next = null;
      }
  }